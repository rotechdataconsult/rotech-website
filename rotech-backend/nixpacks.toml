[variables]
NIXPACKS_PYTHON_VERSION = "3.11"
FRONTEND_URL = "https://rotech-website-production-7cb7.up.railway.app"

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
